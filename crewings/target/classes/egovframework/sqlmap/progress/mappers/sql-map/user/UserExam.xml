<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.sample.user.exam.service.impl.UserExamMapper">
	<select id="getAllList" resultType="hashMap"  parameterType="UserExamVo">
		SELECT * FROM TBL_EXAM
		WHERE 1=1
		<if test="SEARCH_TEXT != ''">
		    <if test="SEARCH_TYPE != 'ALL'">
	    		 AND ${SEARCH_TYPE} LIKE '%${SEARCH_TEXT}%'
	    	</if>
	    </if>
	    <if test="UP != ''">
	    	order by ${UP} asc
	    </if>
	    <if test="DOWN != ''">
	    	order by ${DOWN} desc
	    </if>
	    <if test="DOWN == '' and UP == ''">
	    	order by CREATE_TM desc
	    </if>
	    LIMIT ${LIMIT} OFFSET ${OFFSET}
	</select>
	
	<select id="getAllListCnt" resultType="int" parameterType="UserExamVo">
		SELECT COUNT(*) FROM TBL_EXAM
		WHERE 1=1
		<if test="SEARCH_TEXT != ''">
		    <if test="SEARCH_TYPE != 'ALL'">
	    		 AND ${SEARCH_TYPE} LIKE '%${SEARCH_TEXT}%'
	    	</if>
	    </if> 
	</select>
	
	<insert id="setAdminExamDataInsert" parameterType="UserExamVo">
		INSERT INTO TBL_EXAM (NAME , L_CATEGORY , M_CATEGORY , TYPE , CONTENT , CREATE_TM , UPDATE_TM)
		VALUES(#{name} , #{l_category} , #{m_category} , 0 , #{content} , now() , now())
	</insert>
	
	<update id="setAdminExamDataUpdate" parameterType="UserExamVo">
		UPDATE TBL_EXAM SET
		<if test="name != ''">
			NAME = #{name},
		</if>
		<if test="l_category != ''">
		L_CATEGROY = #{l_category},
		</if>
		<if test="m_category != ''">
		M_CATEGROY = #{m_category},
		</if>
		<if test="type != ''">
		TYPE = #{type},
		</if>
		<if test="content != ''">
		CONTENT = #{content},
		</if>
		UPDATE_TM = now()
		
		WHERE IDX = #{idx}
	</update>
	
	<delete id="setAdminExamDataDelete" parameterType="UserExamVo">
		DELETE FROM TBL_EXAM WHERE IDX = #{idx}
	</delete>
	
	<select id="getExamView" parameterType="UserExamVo" resultType="UserExamVo">
		SELECT * FROM TBL_EXAM
		WHERE IDX = #{idx}
	</select>
	
	<select id="getExamCategoryList" parameterType="UserExamVo" resultType="hashMap">
		<if test="l_category != ''">
			SELECT L_CATEGORY FROM TBL_EXAM
			ORDER BY L_CATEGORY
		</if>
		<if test="m_category != ''">
			SELECT M_CATEGORY FROM TBL_EXAM
			ORDER BY M_CATEGORY
		</if>
	</select>
	
	<!-- question_list 부분 -->
	<select id="getQuestionList" parameterType="UserExamVo" resultType="hashMap">
		SELECT 
		    q.*,
		    GROUP_CONCAT(s.content ORDER BY s.seq SEPARATOR '#') AS Choices
		FROM 
		    tbl_question_list ql
		JOIN 
		    tbl_question q ON ql.question_idx = q.idx
		JOIN 
		    tbl_select s ON q.idx = s.question_idx
		WHERE 
		    ql.exam_idx = #{idx}
		GROUP BY 
		    q.idx
		ORDER BY 
		    ql.seq;
	</select>
	
	<insert id="setAdminExamQuestionListInsert" parameterType="AdminQuestionListVo">
		INSERT INTO TBL_QUESTION_LIST (EXAM_IDX , SEQ , QUESTION_IDX , CREATE_TM , UPDATE_TM)
		VALUES (#{exam_idx} , #{seq} , #{question_idx} , now() , now())
	</insert>
	
	<update id="setAdminExamQuestionListUpdate" parameterType="AdminQuestionListVo">
		UPDATE TBL_QUESTION_LIST SET
		<if test="question_idx != ''">
			QUESTION_IDX = #{question_idx},
		</if>
		UPDATE_TM = now()
		WHERE IDX = #{idx}
	</update>
	
	<delete id="setAdminExamQuestionListDelete" parameterType="AdminQuestionListVo">
		DELETE FROM TBL_QUESTION_LIST
		WHERE IDX = #{idx}
	</delete>


</mapper>